<div class="commentList">
  <% if @post.comments.count > 0 %>
    <% @post.comments.each { |comment|
      @commentData = comment
    %>
      <%= render 'comments/comment' %>
    <% } %>
  <% end %>
</div
